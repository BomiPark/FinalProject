<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.boostcamp.final_project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="final_project"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".UI.PermissionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.NewItem.NewItemActivity"
            android:screenOrientation="portrait"> <!-- 화면 세로 고정 -->
        </activity>
        <activity android:name=".UI.SettingActivity" />
        <activity android:name=".UI.LicenseActivity" />
        <activity
            android:name=".UI.TodoItem.MainActivity"
            android:label=""
            android:theme="@style/AppTheme" />
        <activity android:name=".UI.TodoItem.ItemDetailActivity" />

        <service
            android:name=".Util.GeofenceService"
            android:enabled="true"
            android:exported="true"
            android:process=":my_process" />
        <service
            android:name=".Util.GeofencingService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".UI.FolderItemActivity" />

    </application>

</manifest>